steps:
  # Docker Build
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/${PROJECT_ID}/gcp-devops-gke-pyapp', '.']

images:
  - 'gcr.io/${PROJECT_ID}/gcp-devops-gke-pyapp'

options:
  logging: CLOUD_LOGGING_ONLY